<h1>つぶやき一覧</h1>

<table>
  <tr>
    <th>つぶやき</th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.content %></td>
    <td><%= link_to "つぶやき書き換えちゃう？", edit_post_path(post.id) %></td>
  </tr>
<% end %>
</table>

<%= link_to 'つぶやき投稿へ', new_post_path %>